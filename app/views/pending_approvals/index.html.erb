<div class="min-h-screen bg-gray-50 flex items-center justify-center py-12 px-4">
  <div class="max-w-md w-1000 py-2">
    <div class="bg-white rounded-lg shadow py-8 px-8">
      <div class="text-center px-8">
        <% if @user.inactive? %>
          <!-- アイコン（無効ユーザー） -->
          <div class="mx-auto w-16 h-16 bg-red-100 rounded-full flex items-center justify-center mb-4">
            <svg class="w-8 h-8 text-red-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M18.364 18.364A9 9 0 005.636 5.636m12.728 12.728A9 9 0 015.636 5.636m12.728 12.728L5.636 5.636"></path>
            </svg>
          </div>

          <!-- タイトル -->
          <h1 class="text-2xl font-bold text-gray-900 mb-2">アカウント無効</h1>
          <p class="text-gray-600 mb-6">
            <%= @user.display_name %>さんのアカウントは無効化されています。<br>
            システムへのアクセスが制限されています。
          </p>
        <% else %>
          <!-- アイコン（承認待ち） -->
          <div class="mx-auto w-16 h-16 bg-yellow-100 rounded-full flex items-center justify-center mb-4">
            <svg class="w-8 h-8 text-yellow-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
          </div>

          <!-- タイトル -->
          <h1 class="text-2xl font-bold text-gray-900 mb-2">承認待ち</h1>
          <p class="text-gray-600 mb-6">
            <%= @user.display_name %>さん、システム管理者による承認作業を実施中です。<br>
            承認完了まで今しばらくお待ちください。
          </p>
        <% end %>

        <!-- 連絡先 -->
        <div class="bg-gray-50 rounded-lg p-4 mb-6 text-left">
          <h3 class="text-sm font-medium text-gray-900 mb-2">お急ぎの場合</h3>
          <p class="text-sm text-gray-600">
            <strong>システム管理者:</strong> 情報システム部<br>
            <strong>連絡先:</strong> it-support@company.com
          </p>
        </div>

        <!-- ログアウトボタン -->
        <%= link_to "ログアウト", destroy_user_session_path,
                    data: { turbo_method: :delete },
                    class: "inline-flex items-center px-4 py-2 border border-gray-300 text-sm font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50" %>
      </div>
    </div>
  </div>
</div>
